// By default, terraform init downloads plugins into a subdirectory of the working directory
// so that each working directory is self-contained.
// As a consequence, if you have multiple configurations that use the same provider
// then a separate copy of its plugin will be downloaded for each configuration.
//
// Important: plugin cache path is relative to the TF resource in use
//
plugin_cache_dir = "../plugin-cache"

// When set to true, disables upgrade and security bulletin checks that require reaching out to
// HashiCorp-provided network services.
disable_checkpoint = true